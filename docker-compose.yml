services:
  autobot:
    build: .
    container_name: autobot-v2
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DB_PATH: /app/data/autobot.db
    volumes:
      - ./data:/app/data
      - ./backups:/app/backups
